This is a Next.js MVP for RECtify.

Quickstart:

- Install: `npm install`
- Dev: `npm run dev`
- Open: http://localhost:3000

Leads are stored locally at `data/leads.json` via the `/api/lead` endpoint.
